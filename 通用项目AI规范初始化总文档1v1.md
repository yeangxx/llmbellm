# 通用项目 AI 规范初始化总文档

本文档用于任意软件项目。用户把本文件放到项目根目录或交给 AI 后，AI 应先侦察项目，再为项目建立一套可持续使用的 AI 开发规范体系。

目标不是生成一堆静态模板，而是让后续协作变成稳定流程：用户用自然语言讨论需求，AI 形成需求、方案、设计和开发计划，之后用户只需要一句短口令即可开始或继续执行。

所有由本文档生成或维护的 Markdown 文档，统一使用 `UTF-8` 编码。

## 1. 用户最短用法

把本文件放到项目根目录或发给 AI，然后说：

```text
按《通用项目 AI 规范初始化总文档》初始化本项目的 AI 开发规范体系。
```

初始化完成后，日常开发可以说：

```text
继续当前任务
```

生成新开发计划后，可以说：

```text
按这个开发计划执行
```

如果明确是低风险小改，可以说：

```text
小改：修复用户列表空状态文案不准确的问题
按小改流程执行：把登录按钮文案从“提交”改成“登录”
```

“小改”短口令只表示进入小改判定流程，不表示 AI 可以绕过检查直接修改文件。

如果只想讨论、不希望 AI 修改文件，可以说：

```text
先不要改代码，我们只讨论。
```

AI 不得要求用户每次重复规范路径、阶段规则、UI 规范、后端规范、验证规则或文档同步规则。

## 2. 初始化档位

不同项目规模不同，AI 不应默认把完整治理体系硬套到所有项目。初始化前应先判断项目规模，并说明采用的档位。

### 2.1 轻量模式

适用于空项目、脚本项目、临时工具、小型 Demo。

必须生成或更新：

```text
AGENTS.md
docs/README.md
docs/00-文档治理/项目上下文锚点.md
docs/07-规划路线/AI当前执行任务.md
docs/07-规划路线/AI当前执行任务模板.md
docs/07-规划路线/AI小改任务.md
docs/05-测试文档/项目验证命令与测试策略.md
docs/10-开发规范/项目特殊开发规范.md
```

轻量模式只沉淀项目事实、项目目标、短口令规则、验证命令和当前任务状态，不强制生成全部模板。项目涉及 UI 时，还必须生成或更新 `docs/08-UI设计/UI设计基线与交互规范.md`。

### 2.2 标准模式

适用于多数真实业务项目。除轻量模式文件外，还应补齐：

```text
docs/00-文档治理/
docs/01-需求文档/
docs/02-解决方案/
docs/03-设计文档/
docs/04-功能实现/
docs/05-测试文档/
docs/06-决策记录/
docs/07-规划路线/
docs/08-UI设计/
docs/09-AI执行规范/
docs/10-开发规范/
docs/99-历史归档/
```

标准模式是默认推荐档位。

### 2.3 完整模式

适用于多人长期维护项目、复杂前后端项目、需要严格审计或阶段化交付的项目。

除标准模式内容外，还应完整生成需求、方案、设计、计划、实现、测试、决策、UI、文档同步、阶段收尾、初始化验收等模板，并把 UI、后端、数据、权限、部署规则细化到可执行程度。

### 2.4 档位选择规则

```text
用户明确指定档位时，按用户指定执行。
未指定时，默认采用标准模式。
项目为空或只有少量脚本时，可以建议轻量模式，但应说明原因。
项目已有复杂 docs、CI、部署、数据库或多模块结构时，应采用完整模式或标准模式。
```

## 3. 核心硬规则

无论采用哪个档位，AI 都必须遵守以下规则：

```text
1. 先侦察，后创建或修改文件。
2. 先记录项目事实，再制定项目规范。
3. 已有规范必须合并，不得直接覆盖。
4. 发现规范冲突、项目事实冲突或高风险变更时，必须停止并说明。
5. 没有真实运行过的命令，不能写成已通过。
6. 初始化只修改规范和文档，不改业务代码。
7. 当前任务状态文件是继续任务的权威入口，不能只依赖聊天上下文。
8. 短口令触发后，AI 必须重新读取关键文档。
9. 文档中未知的信息必须写“未确认”或“待验证”，不得编造。
10. 所有生成文档使用 UTF-8 编码。
11. 长期任务必须通过文件恢复上下文，不能依赖 AI 记忆。
12. 项目目的、用户目标、业务边界、UI 基线和不可破坏约束必须沉淀为上下文锚点。
13. 有开发计划不等于可以开工，必须先通过开发计划执行前完备性门禁。
```

### 3.1 长期上下文防漂移机制

AI 最大的风险不是不知道怎么写代码，而是开发时间一长后忘记“为什么做、给谁做、按什么边界做、UI 应该像什么”。因此项目必须建立上下文锚点。

上下文锚点不是普通说明文档，而是每次继续任务前必须重读的权威材料。

必须建立或维护：

```text
docs/00-文档治理/项目上下文锚点.md
docs/08-UI设计/UI设计基线与交互规范.md
docs/10-开发规范/项目特殊开发规范.md
docs/07-规划路线/AI当前执行任务.md
```

`项目上下文锚点.md` 至少包含：

```text
项目一句话目标：
目标用户：
核心使用场景：
当前系统开发目的：
本阶段最重要目标：
明确不做的事：
核心业务边界：
不可破坏的兼容面：
核心数据/接口/权限约束：
关键设计决策摘要：
当前风险和未确认事项：
```

`UI设计基线与交互规范.md` 只在项目涉及 UI 时必须生成或更新，至少包含：

```text
产品气质：
主要页面类型：
信息层级原则：
布局原则：
组件使用原则：
颜色、间距、圆角、阴影原则：
动效原则：
响应式原则：
空状态、错误态、加载态原则：
禁止出现的 UI 风格：
已有页面或组件参考：
```

每次看到“继续当前任务”“继续”“下一步”“按当前计划开发”等短口令时，AI 必须先执行上下文恢复：

```text
1. 读取 AGENTS.md
2. 读取 docs/README.md
3. 读取 docs/00-文档治理/项目上下文锚点.md
4. 读取 docs/10-开发规范/项目特殊开发规范.md
5. 如涉及 UI，读取 docs/08-UI设计/UI设计基线与交互规范.md
6. 读取 docs/07-规划路线/AI当前执行任务.md
7. 读取 active_plan / active_requirement / 当前阶段依赖文档
8. 对照当前请求，判断是否偏离项目目的、UI 基线或阶段边界
```

继续任务前必须输出或内部完成漂移自检：

```text
是否符合项目一句话目标：
是否符合当前系统开发目的：
是否仍在当前阶段 allowed_scope 内：
是否触碰 forbidden_scope：
是否符合项目特殊开发规范：
如涉及 UI，是否符合 UI 基线：
是否需要更新上下文锚点：
是否发现旧计划和当前事实冲突：
```

如果 AI 无法找到上下文锚点，或发现当前计划与项目目的、UI 基线、业务边界明显冲突，必须停止并让用户确认。

### 3.2 精准上下文读取、证据链与实时进度规则

长期开发时，AI 必须同时保证方向准确、依据可追溯、进度透明。核心原则：

```text
精准第一，token 第二。
摘要用于定位，决策必须看原文，编码必须看代码。
聊天里短频更新，状态文件里记录关键里程碑。
```

AI 每次继续任务时，必须按文档权威等级读取材料：

```text
A0 核心锚点：每次继续任务必须原文读取
- AGENTS.md
- docs/README.md
- docs/00-文档治理/项目上下文锚点.md
- docs/07-规划路线/AI当前执行任务.md
- docs/10-开发规范/项目特殊开发规范.md
- 涉及 UI 时：docs/08-UI设计/UI设计基线与交互规范.md

A1 当前任务依据：当前阶段必须原文读取
- active_plan 指向的开发计划
- active_requirement 指向的需求文档
- 当前阶段依赖的方案、设计、测试、UI、决策文档

A2 事实依据：实现前必须读取
- 当前阶段相关代码
- package scripts / CI / README / 配置文件
- 接口、数据库、权限、部署、环境变量相关真实文件

A3 导航材料：只用于定位
- docs/README.md
- 模板目录
- 文档索引
- 历史归档索引

A4 历史和模板：按需读取
- 历史归档
- 通用模板
- 非当前阶段文档
```

AI 不得只根据 A3/A4 材料做实现决策。

看到短口令时，必须执行精准读取流程：

```text
1. 原文读取 A0 核心锚点。
2. 从 AI当前执行任务.md 读取 active_plan、active_requirement、current_stage。
3. 原文读取 A1 当前任务依据。
4. 判断任务是否涉及 UI / 后端 / 数据 / 权限 / 部署 / 测试。
5. 按条件读取对应规范和真实代码。
6. 输出或内部完成上下文漂移自检。
7. 通过证据链开工检查后才能修改文件。
```

每次修改代码、文档、样式、接口、数据结构或测试前，AI 必须建立证据链：

```text
项目目的依据：
当前阶段依据：
需求依据：
设计依据：
UI 依据：
后端/数据/权限/部署依据：
实际代码依据：
验证依据：
```

如果关键依据缺失，不能继续猜测，必须升级读取或停止确认。

AI 必须维护两类进度：

```text
会话内进度：
在当前对话中简短说明正在做什么、已完成什么、下一步做什么、是否遇到风险。

文件内进度：
在 AI当前执行任务.md 中记录关键里程碑，不能只靠聊天保存进度。
```

以下节点必须标注进度：

```text
1. 完成上下文读取后
2. 完成计划解析后
3. 开始修改文件前
4. 完成一组相关文件修改后
5. 开始验证前
6. 验证完成后
7. 文档同步完成后
8. 遇到阻塞、冲突、失败或范围变化时
9. 阶段完成等待用户确认时
```

遇到以下情况，AI 必须扩大读取范围：

```text
需求和当前计划不一致
代码现状和设计文档不一致
涉及公共接口、数据库、权限、部署
UI 效果无法从 UI 基线判断
测试失败原因不明确
active_plan 或 active_requirement 信息不足
用户提出的新要求可能改变项目目的或阶段范围
```

升级读取后仍不确定，必须停止并询问用户。

### 3.3 开发计划执行前完备性门禁

开发计划存在不等于可以执行。AI 看到下面短口令时，不能立刻进入实现：

```text
按这个开发计划执行
按当前计划开发
继续当前任务
进入下一阶段
```

AI 必须先执行开工前完备性门禁。只有门禁通过，才允许进入实现阶段。

门禁必须检查：

```text
项目目标是否明确
active_plan 是否存在且路径明确
active_requirement 是否存在且路径明确
current_stage 是否明确
allowed_scope / forbidden_scope 是否明确
planned_outputs 是否明确
validation_policy 是否明确
必备需求文档是否完整
必备设计文档是否完整
涉及 UI 时 UI 基线是否存在、完整且可信
涉及后端时接口 / 数据 / 权限边界是否明确
涉及部署时部署影响、回滚方式和验证方式是否明确
验收标准和验证方式是否明确
哪些内容是用户确认，哪些只是 AI 推断
是否存在阻塞问题或待确认问题
```

涉及 UI 时，门禁至少检查：

```text
页面目标：
目标用户：
主流程：
主操作：
次操作：
信息层级：
UI 气质：
组件风格：
状态覆盖：loading / empty / error / disabled / success
响应式要求：
验收标准：
是否沿用现有 UI：
哪些内容已由用户确认：
哪些内容只是 AI 推断：
```

AI 可以生成候选草案，但候选草案必须标记为：

```text
AI 推断 / 待用户确认
```

未确认的 AI 推断不得作为强执行依据。

如果门禁缺少必备条件，AI 必须：

```text
1. 停止进入实现。
2. 把 status 标记为 blocked 或 waiting_user_confirm。
3. 把 progress_status 标记为 waiting_user_input。
4. 记录 missing_items、pending_questions、ai_inferred_items。
5. 每轮最多提出 1-3 个关键问题。
6. 用户回答后更新相关文档和当前任务状态。
7. 重新执行完备性门禁。
8. 直到 required_inputs_complete = true 且 user_confirmed_ready_to_execute = true 后，才允许开工。
```

门禁通过标准：

```text
execution_gate.status = passed
required_inputs_complete = true
user_confirmed_ready_to_execute = true
missing_items = []
pending_questions = []
```

门禁未通过时，AI 不得修改业务代码、样式、接口、数据结构、权限模型或部署配置。

### 3.4 小改快速通道

小改快速通道用于低风险、目标明确、影响范围极小的修改。小改不是跳过规范，而是使用更短的规范路径，避免把简单修复强行推进为完整需求、方案、设计和开发计划。

用户必须显式使用下面短口令之一，AI 才能进入小改判定流程：

```text
小改：<具体改动>
只做小改：<具体改动>
按小改流程执行：<具体改动>
```

用户未显式使用“小改”短口令时，AI 不得仅凭主观判断自动降级为小改流程。如果 AI 认为当前请求更适合小改，只能建议用户改用小改短口令或询问用户确认。

看到“小改”短口令后，AI 不得直接编码，必须先执行小改判定。小改必须同时满足：

```text
1. 目标一句话能说清。
2. 影响范围可限定到少量文件。
3. 不改公共接口。
4. 不改数据库结构。
5. 不改权限模型。
6. 不改部署方式。
7. 不改变核心业务流程。
8. 验证方式明确。
9. 可以通过局部回滚恢复。
10. 不需要长期分阶段推进。
```

只要触发下面任意一项，AI 必须停止小改流程，并说明需要升级为标准开发计划流程或完整流程：

```text
接口变更
数据库变更
权限变更
部署变更
多模块联动
核心业务流程变化
UI 状态、交互或动画目标不明确
需求存在歧义
影响现有开发流程或验证基线
需要长期分阶段推进
无法明确验证方式
无法明确回滚方式
```

小改流程必须创建或更新小改任务卡：

```text
docs/07-规划路线/AI小改任务.md
```

小改任务卡必须包含机器可读 YAML frontmatter，建议格式如下：

```yaml
---
task_type: "small_change"
status: "ready"
goal: ""
allowed_files: []
forbidden_files: []
risk_level: "low"
requires_full_plan: false
api_change: false
database_change: false
permission_change: false
deployment_change: false
ui_change: false
validation_commands: []
rollback_note: ""
missing_items: []
pending_questions: []
user_confirmed_scope: false
last_updated: "YYYY-MM-DD"
---
```

正文必须包含：

```text
小改目标
允许修改范围
禁止修改范围
相关代码依据
验证方式
回滚说明
完成记录
```

小改开工前，AI 必须输出简化开工检查：

```text
当前流程：小改快速通道
小改目标：
是否显式使用小改短口令：
是否满足小改判定条件：
是否只影响 allowed_files：
是否涉及接口/数据库/权限/部署：
是否涉及 UI：
是否需要升级为完整开发计划：
验证命令：
回滚方式：
是否可以开始修改：
```

小改流程允许跳过完整需求、方案、设计和开发计划文档，但不能跳过以下动作：

```text
1. 读取 AGENTS.md、docs/README.md、项目上下文锚点、项目特殊开发规范。
2. 读取或创建 docs/07-规划路线/AI小改任务.md。
3. 读取与 allowed_files 相关的真实代码或文档。
4. 涉及 UI 时读取 UI 基线。
5. 涉及验证时读取项目验证命令与测试策略。
6. 修改前记录允许范围、禁止范围、验证方式和回滚说明。
7. 修改后真实运行可执行的验证命令，或如实记录未运行原因。
8. 更新小改任务卡的完成记录。
```

小改任务完成后，AI 必须说明：

```text
是否仍符合小改范围
实际修改文件
验证命令和结果
未验证项
是否需要同步长期文档
是否需要升级为标准开发计划流程
```

## 4. 初始化前必须先侦察项目

AI 在创建或修改文件前必须只读侦察：

```text
项目根目录结构
是否已有 AGENTS.md / CLAUDE.md / .github/copilot-instructions.md 等 AI 指令
是否已有 docs 目录
技术栈：package.json / pnpm-workspace.yaml / pyproject.toml / Cargo.toml / go.mod / pom.xml / build.gradle 等
前端入口：app / pages / src / components / styles 等
后端入口：server / api / controllers / services / workers / packages 等
测试命令：package scripts、CI 配置、README
部署入口：Dockerfile、docker-compose、k8s、scripts、README
数据库或迁移目录
权限、安全、配置和环境变量位置
已有文档、开发规范、架构说明、历史决策记录
```

如果项目已有规范文件，AI 不能直接覆盖。必须：

```text
读取已有内容
保留已有规则
补充缺失规则
发现冲突时列出冲突并停止确认
```

## 5. 已存在项目必须先形成项目画像

如果项目已经存在代码、配置、部署脚本或历史文档，AI 不能只复制模板。必须先把当前项目事实沉淀到文档中。

### 5.1 必须生成或更新的项目画像文档

```text
docs/03-设计文档/项目架构与模块边界.md
docs/10-开发规范/项目特殊开发规范.md
docs/05-测试文档/项目验证命令与测试策略.md
docs/07-规划路线/项目启动与部署说明.md
```

如果项目已有同类文档，AI 应更新已有文档，不重复创建近义文档。

### 5.2 项目画像必须记录

```text
项目名称：
主要技术栈：
包管理器：
前端框架：
后端框架：
数据库：
ORM / 迁移工具：
任务队列 / worker：
缓存：
搜索：
文件存储：
鉴权方式：
配置文件位置：
环境变量来源：
本地启动命令：
测试命令：
类型检查命令：
构建命令：
部署入口：
Docker / compose / k8s：
CI 配置：
核心目录：
公共模块：
禁止随意修改的兼容面：
已有规范文件：
已知基线失败：
未确认事项：
```

### 5.3 技术栈识别依据

AI 必须优先从真实文件识别，而不是凭项目名猜测：

```text
package.json / pnpm-workspace.yaml / yarn.lock / package-lock.json
pyproject.toml / requirements.txt / poetry.lock
Cargo.toml
go.mod
pom.xml / build.gradle
Dockerfile / docker-compose*.yml
.github/workflows/*
README.md
scripts/*
src / app / apps / packages / services / workers
database / migrations / prisma / drizzle / sequelize / alembic
```

### 5.4 现有项目适配原则

```text
先记录事实，再制定规范。
先兼容现有目录，再建议重命名或迁移。
先读取已有脚本，再定义新的启动和验证命令。
先识别已有 UI 风格，再叠加通用 UI 规范。
先识别已有后端分层，再叠加后端规范。
先识别已有测试基线，再规定验证方式。
不能把模板规范硬套到现有项目导致目录混乱。
不能删除或覆盖已有项目文档。
```

## 6. 推荐目录结构

标准模式和完整模式推荐创建或补齐：

```text
docs/
├─ 00-文档治理/
├─ 01-需求文档/
├─ 02-解决方案/
├─ 03-设计文档/
├─ 04-功能实现/
├─ 05-测试文档/
├─ 06-决策记录/
├─ 07-规划路线/
├─ 08-UI设计/
├─ 09-AI执行规范/
├─ 10-开发规范/
└─ 99-历史归档/
```

如果项目已经有不同目录命名，AI 应优先兼容现有结构；只有用户确认后才迁移或重命名。

## 7. 必须生成的核心文件

### 7.1 根入口

```text
AGENTS.md
```

用途：

```text
仓库级 AI 开发入口
规定必读顺序
规定短口令触发规则
规定编码前检查
规定 UI / 后端 / 验证 / 文档同步硬约束
```

### 7.2 文档中心

```text
docs/README.md
```

用途：

```text
解释 docs 目录结构
说明默认推进顺序
链接 AI 执行规范和当前任务状态
说明不同初始化档位下哪些文档是必需项
```

### 7.3 文档治理

标准模式和完整模式应生成：

```text
docs/00-文档治理/AI需求驱动开发流程.md
docs/00-文档治理/功能交付统一流程.md
docs/00-文档治理/代码变更文档同步规范.md
docs/00-文档治理/初始化验收清单.md
```

用途：

```text
用户自然语言 -> AI 整理需求 -> 方案 -> 设计 -> 计划 -> 实现 -> 验证 -> 文档同步
```

### 7.4 AI 执行规范

```text
docs/09-AI执行规范/README.md
docs/09-AI执行规范/AI阶段化开发入口.md
```

用途：

```text
规定“继续当前任务”时 AI 每次必须重读哪些文档
规定如何找到 active_plan / active_requirement / current_stage
规定如何识别前序阶段产物
规定如何维护当前任务状态
规定如何处理停止条件和确认条件
```

### 7.5 当前任务状态

```text
docs/07-规划路线/AI当前执行任务.md
docs/07-规划路线/AI当前执行任务模板.md
docs/07-规划路线/AI小改任务.md
```

用途：

```text
记录当前唯一 active_plan
记录 current_stage / next_stage / completed_stages
记录阶段允许范围、禁止范围、验证策略
记录每阶段完成结果
防止 AI 误继续旧计划
记录低风险小改的目标、允许范围、验证方式和完成结果
```

### 7.6 长期上下文锚点

```text
docs/00-文档治理/项目上下文锚点.md
docs/08-UI设计/UI设计基线与交互规范.md
```

用途：

```text
沉淀项目开发目的、用户目标、业务边界和不可破坏约束
沉淀 UI 风格、交互状态、响应式和动效基线
作为长时间开发后的上下文恢复入口
防止 AI 偏离项目目的、系统边界或既定 UI 风格
```

如果项目不涉及 UI，可以不创建 `UI设计基线与交互规范.md`，但必须在 `项目上下文锚点.md` 中记录“当前项目无 UI 交付面”。

### 7.7 通用模板

标准模式和完整模式应生成：

```text
docs/01-需求文档/需求文档模板.md
docs/02-解决方案/解决方案模板.md
docs/03-设计文档/设计文档模板.md
docs/04-功能实现/实现记录模板.md
docs/05-测试文档/测试记录模板.md
docs/06-决策记录/决策记录模板.md
docs/07-规划路线/开发计划模板.md
docs/08-UI设计/UI设计模板.md
docs/10-开发规范/通用开发规范.md
docs/10-开发规范/开发规范分层与覆盖规则.md
```

## 8. AGENTS.md 必须包含的规则

AI 应把下面内容写入或合并到 `AGENTS.md`：

```text
本文件是仓库级 AI 开发入口。任何 AI 修改代码、文档、样式、接口、数据结构或测试前，必须先阅读并遵守本文件。

必读顺序：
1. docs/README.md
2. docs/09-AI执行规范/README.md
3. docs/09-AI执行规范/AI阶段化开发入口.md
4. docs/00-文档治理/AI需求驱动开发流程.md
5. docs/00-文档治理/功能交付统一流程.md
6. docs/00-文档治理/代码变更文档同步规范.md
7. docs/00-文档治理/项目上下文锚点.md
8. docs/10-开发规范/通用开发规范.md
9. docs/10-开发规范/开发规范分层与覆盖规则.md
10. docs/10-开发规范/项目特殊开发规范.md
11. 如涉及 UI，读取 docs/08-UI设计/UI设计基线与交互规范.md

短口令：
- 继续当前任务
- 继续
- 下一步
- 按当前计划开发
- 进入下一阶段
- 按这个开发计划执行
- 小改：<具体改动>
- 只做小改：<具体改动>
- 按小改流程执行：<具体改动>

看到继续类或计划执行类短口令时，AI 必须自动读取：
- AGENTS.md
- docs/README.md
- docs/00-文档治理/项目上下文锚点.md
- docs/09-AI执行规范/README.md
- docs/09-AI执行规范/AI阶段化开发入口.md
- docs/10-开发规范/项目特殊开发规范.md
- 如涉及 UI，读取 docs/08-UI设计/UI设计基线与交互规范.md
- docs/07-规划路线/AI当前执行任务.md
- active_plan 指向的开发计划
- active_requirement 指向的需求文档
- 当前阶段依赖文档
- 已完成阶段产物
- 当前阶段相关代码

看到小改类短口令时，AI 必须自动读取：
- AGENTS.md
- docs/README.md
- docs/00-文档治理/项目上下文锚点.md
- docs/10-开发规范/项目特殊开发规范.md
- docs/07-规划路线/AI小改任务.md
- docs/05-测试文档/项目验证命令与测试策略.md
- 如涉及 UI，读取 docs/08-UI设计/UI设计基线与交互规范.md
- 与 allowed_files 或目标改动相关的真实代码

AI 不得只依赖聊天上下文，不得只读取开发计划。

看到“按这个开发计划执行”“按当前计划开发”“继续当前任务”“进入下一阶段”时，AI 必须先执行开工前完备性门禁。门禁未通过时，只能追问、补齐文档和更新状态，不得进入实现。

看到“小改”短口令时，AI 必须先执行小改判定。判定通过后，只允许按小改任务卡限定的 allowed_files 和验证方式执行；判定不通过时，必须停止并说明需要升级为标准开发计划流程或完整流程。
```

如果项目已经存在 `AGENTS.md`，必须合并上述规则，不得覆盖原有内容。

## 9. 当前任务状态文件格式

`docs/07-规划路线/AI当前执行任务.md` 必须包含机器可读的 YAML frontmatter，建议格式如下：

```yaml
---
active_plan: ""
active_requirement: ""
context_anchor: "docs/00-文档治理/项目上下文锚点.md"
ui_baseline: ""
required_reading:
  - "AGENTS.md"
  - "docs/README.md"
  - "docs/00-文档治理/项目上下文锚点.md"
  - "docs/07-规划路线/AI当前执行任务.md"
  - "docs/10-开发规范/项目特殊开发规范.md"
conditional_reading:
  ui:
    - "docs/08-UI设计/UI设计基线与交互规范.md"
  backend:
    - "docs/03-设计文档/项目架构与模块边界.md"
  validation:
    - "docs/05-测试文档/项目验证命令与测试策略.md"
execution_gate:
  status: "not_checked"
  required_inputs_complete: false
  user_confirmed_ready_to_execute: false
  missing_items: []
  pending_questions: []
  ai_inferred_items: []
  confirmed_items: []
  last_checked: "YYYY-MM-DD HH:mm"
task_name: ""
task_level: "small | medium | large"
status: "ready"
progress_status: "not_started"
progress_percent: 0
current_stage: ""
next_stage: ""
current_step: ""
next_step: ""
last_completed_stage: ""
completed_stages: []
last_progress_update: "YYYY-MM-DD HH:mm"
last_updated: "YYYY-MM-DD"
evidence_required: true
---
```

允许的 `status`：

```text
ready
in_progress
waiting_user_confirm
blocked
completed
archived
```

建议的 `progress_status`：

```text
not_started
reading_context
checking_execution_gate
waiting_user_input
planning
implementing
validating
documenting
waiting_user_confirm
blocked
completed
```

正文必须包含：

```text
阶段依赖文档
长期上下文锚点
本轮必读清单
条件必读清单
开工前完备性门禁记录
当前阶段 allowed_scope
当前阶段 forbidden_scope
当前阶段 planned_outputs
当前阶段 validation_policy
当前阶段 stop_conditions
实时进度记录
阶段结果记录
每次继续强制重读清单
上下文漂移自检记录
切换规则
```

正文中的实时进度记录建议格式：

```text
## 实时进度记录

- 时间：
  progress_status：
  progress_percent：
  已完成：
  正在进行：
  下一步：
  风险：
  是否需要用户确认：
```

状态机：

```text
ready
  -> in_progress
  -> waiting_user_confirm
  -> ready(next stage)
  -> ...
  -> completed
```

阶段开始前：

```text
status = in_progress
last_updated = 当前日期
```

阶段完成后：

```text
status = waiting_user_confirm
last_completed_stage = 当前阶段
completed_stages 增加当前阶段
记录 changed_files / updated_documents / validation_run / validation_not_run / remaining_risks
```

用户确认后：

```text
current_stage = next_stage
status = ready
```

全部完成后：

```text
status = completed
current_stage = 无
next_stage = 无
```

## 10. AI 阶段化开发入口必须包含的规则

`docs/09-AI执行规范/AI阶段化开发入口.md` 至少包含：

```text
1. 短口令规则
2. 权威顺序
3. 当前任务状态读取规则
4. 继续任务强制重读清单
5. 开发计划解析规则
6. 关联文档自动发现规则
7. 当前阶段定位规则
8. 前序阶段产物识别规则
9. UI / 动画 / 交互自动识别规则
10. 后端 / 数据 / 权限 / 部署影响识别规则
11. 开工检查表
12. 阶段状态机
13. 阶段收尾模板
14. 文档同步规则
15. 验证规则
16. 停止条件与确认条件
17. 长期上下文恢复规则
18. 项目目的、UI 基线和阶段边界漂移自检规则
19. 文档权威等级规则
20. 证据链开工检查规则
21. 实时进度标注与状态同步规则
22. 读取升级规则
23. 开发计划执行前完备性门禁规则
24. 缺失条件追问、补齐和重新检查规则
25. 小改快速通道、判定条件、任务卡和升级规则
```

## 11. 关联文档自动发现规则

AI 不应要求用户手动列出需求、方案、设计、测试和 UI 文档。必须按下面顺序自动找：

```text
1. 开发计划中的关联文档列表
2. AI当前执行任务.md 中记录的阶段依赖文档
3. 根据开发计划文件名、任务名、需求名称和核心关键词，在 docs 各目录检索
4. 如果只有一个高可信候选，采用并记录
5. 如果多个候选或没有候选，停止并让用户确认
```

检索目录：

```text
docs/01-需求文档/
docs/02-解决方案/
docs/03-设计文档/
docs/05-测试文档/
docs/06-决策记录/
docs/08-UI设计/
```

## 12. UI、动画和交互自动识别规则

只要用户需求、开发计划、阶段目标、计划修改文件、代码搜索结果或实际改动包含下面内容，AI 必须判定为涉及 UI / 动画 / 交互：

```text
页面、组件、按钮、表单、弹窗、抽屉、菜单、列表、表格、卡片、导航、布局、响应式、移动端、样式、颜色、间距、圆角、阴影、图标、hover、focus、pressed、loading、empty、error、disabled、success、动画、动效、过渡、进入退出、展开收起、拖拽、排序、Material、Google、motion、transition、animation、framer-motion、CSS、Tailwind、className
```

如果计划修改文件包含下面路径或文件类型，AI 必须先检查是否实际涉及 UI。涉及 UI 时按 UI 任务处理；如果只是纯逻辑或类型调整，应说明判定依据。

```text
app/
pages/
src/components/
components/
styles/
*.tsx
*.jsx
*.vue
*.svelte
*.css
*.scss
*.module.css
tailwind.config.*
```

判定为 UI 后，必须读取 UI 规范和模板，并输出 UI 草案：

```text
页面目标：
主操作：
次操作：
信息层级：
组件结构：
状态清单：
动画清单：
Material 参考点：
响应式策略：
验证方式：
```

UI 实现必须覆盖：

```text
default
hover
focus
pressed
loading
disabled
empty
error
success
```

动画必须支持 `prefers-reduced-motion`。如果现有项目已有设计系统或组件库，应优先遵守现有系统。

## 13. 后端、数据、权限和部署自动识别规则

只要涉及下面内容，AI 必须判定为后端或架构影响：

```text
接口、API、Controller、Service、Repository、数据库、迁移、schema、事务、权限、鉴权、角色、token、secret、配置、环境变量、任务、队列、worker、runtime、engine、plugin、adapter、provider、pipeline、部署、Docker、CI、缓存、消息、文件读写、搜索、模型调用、外部服务
```

AI 必须自动读取后端设计规范或生成项目适配版后端规范，并在开工检查中说明：

```text
接口兼容点：
数据兼容点：
权限影响：
部署影响：
回滚方式：
验证方式：
```

如果接口、数据库、权限或部署变更已经在开发计划中明确写出，并且用户已经确认该计划，AI 可以继续执行，但必须记录兼容、回滚和验证方案。

如果这些变更没有在计划中明确写出，AI 必须停止并让用户确认。

## 14. 开工检查表

每个会修改代码、文档、样式、接口、数据结构或测试的阶段，AI 开始前必须输出：

```text
当前阶段：
任务级别：
execution_gate 状态：
required_inputs_complete：
user_confirmed_ready_to_execute：
missing_items：
pending_questions：
本轮决策依据：
项目目的依据：
当前阶段依据：
需求依据：
设计依据：
UI 依据：
后端/数据/权限/部署依据：
实际代码依据：
验证依据：
已阅读文档：
已阅读代码：
项目目的对齐：
上下文锚点对齐：
影响范围：
计划修改文件：
不改范围：
接口兼容点：
数据兼容点：
权限影响：
前端影响：
部署影响：
验证方式：
是否存在上下文漂移风险：
是否可以开始编码：
```

如果只是初始化规范文档，仍需输出简化版开工检查，至少说明将创建或更新哪些规范文件、不会修改哪些业务代码。

## 15. 阶段收尾模板

每个阶段完成后，AI 必须更新当前任务状态，并输出：

```text
stage_result：
last_completed_stage：
completed_stages：
changed_files：
updated_documents：
documents_not_updated_and_reason：
context_anchor_updated：
ui_baseline_updated：
execution_gate_status：
validation_run：
validation_result：
validation_not_run：
remaining_risks：
next_stage：
progress_status：waiting_user_confirm
status：waiting_user_confirm
last_progress_update：
last_updated：
```

## 16. 文档同步规则

代码或文档变更后，AI 必须判断是否需要同步：

```text
需求文档
解决方案
设计文档
实现记录
测试记录
决策记录
规划路线
UI 设计
AI 执行规范
开发规范
历史归档
```

没有同步的必须说明原因。文档同步不得编造未验证事实。

## 17. 验证规则

AI 只能把真实运行过的命令写成通过。未运行、运行失败、环境缺失、命令不存在，都必须如实记录。

每次交付至少说明：

```text
验证命令：
验证结果：
失败项：
是否由本次改动引起：
未验证项：
剩余风险：
```

初始化阶段的验证至少应检查：

```text
必需文件是否存在
项目上下文锚点是否存在
涉及 UI 时 UI 基线是否存在
文档权威等级规则是否可检索
证据链开工检查规则是否可检索
实时进度标注规则是否可检索
读取升级规则是否可检索
开发计划执行前完备性门禁规则是否可检索
缺失条件追问和重新检查规则是否可检索
短口令规则是否可检索
状态机规则是否可检索
继续任务强制重读清单是否可检索
上下文恢复和漂移自检规则是否可检索
项目画像文档是否已生成或更新
已有规范是否未被覆盖
未知信息是否标记为“未确认”或“待验证”
```

## 18. 停止条件与确认条件

### 18.1 必须立即停止

遇到下面情况，AI 必须停止并说明原因：

```text
用户明确要求只讨论或先不要改
需求不明确，无法安全限定范围
开发计划缺失或路径不明确
当前任务状态文件缺失且无法安全初始化
前序阶段产物缺失
实际代码和计划冲突
已有规范之间存在冲突
需要执行破坏性操作
涉及 secret、凭据、安全策略但缺少上下文
找不到项目上下文锚点，且无法从现有文档安全恢复项目目的
当前计划明显偏离项目目的、业务边界或 UI 基线
开工前完备性门禁未通过
缺少必备文档、必备条件或用户确认状态
验证失败且无法判断原因
```

### 18.2 必须确认后继续

遇到下面情况，如果未在已确认计划中明确声明，AI 必须让用户确认：

```text
需要改公共接口
需要改数据库结构
需要改权限模型
需要改变部署方式
影响范围超过当前阶段
UI 状态、交互或动画目标不明确
会迁移、重命名或重排已有文档目录
会改变现有开发流程或验证基线
```

## 19. 初始化验收标准

AI 完成初始化后，必须检查：

```text
AGENTS.md 存在
docs/README.md 存在
docs/00-文档治理/项目上下文锚点.md 存在
docs/09-AI执行规范/AI阶段化开发入口.md 存在
docs/07-规划路线/AI当前执行任务.md 存在
docs/07-规划路线/AI当前执行任务模板.md 存在
docs/07-规划路线/AI小改任务.md 存在
docs/05-测试文档/项目验证命令与测试策略.md 存在
docs/10-开发规范/项目特殊开发规范.md 存在
涉及 UI 时 docs/08-UI设计/UI设计基线与交互规范.md 存在
标准模式或完整模式下 docs/00-文档治理 关键文件存在
标准模式或完整模式下需求/方案/设计/计划/实现/测试/决策/UI 模板存在
短口令规则可检索
状态机规则可检索
UI 自动识别规则可检索
后端/数据/权限/部署识别规则可检索
每次继续强制重读清单可检索
上下文恢复规则可检索
漂移自检规则可检索
文档权威等级规则可检索
证据链开工检查规则可检索
实时进度标注规则可检索
读取升级规则可检索
开发计划执行前完备性门禁规则可检索
execution_gate 字段可检索
缺失条件追问、补齐和重新检查规则可检索
小改快速通道规则、判定条件、任务卡和升级规则可检索
已存在项目的项目画像文档已生成或更新
验证命令与测试策略已生成或更新
项目启动与部署说明已生成或更新
未知信息已标记“未确认”或“待验证”
```

初始化完成后，AI 必须告诉用户：

```text
本次采用的初始化档位
创建或更新了哪些文件
哪些已有文件被合并，未覆盖哪些内容
哪些信息仍未确认或待验证
项目目的和 UI 基线记录在哪里
以后可以说：继续当前任务
新计划生成后可以说：按这个开发计划执行
低风险小改可以说：小改：<具体改动>
如果只想讨论，可以说：先不要改代码，我们只讨论
```

## 20. 给任意项目 AI 的初始化提示词

用户可以复制下面这段给任何项目里的 AI：

```text
请严格按照《通用项目 AI 规范初始化总文档》初始化本项目。

目标：
1. 先只读侦察当前项目技术栈、目录、测试命令、部署入口和已有规范。
2. 根据项目规模选择轻量、标准或完整初始化模式；未指定时默认采用标准模式。
3. 如果是已存在项目，先生成或更新项目画像：架构边界、项目特殊规范、验证命令、启动与部署说明。
4. 创建或补齐 AGENTS.md、docs 目录、项目上下文锚点、AI执行规范、阶段化开发入口、当前任务状态、当前任务模板、小改任务卡、需求/方案/设计/计划/实现/测试/UI/决策模板。
5. 保留已有规范，不得直接覆盖；发现冲突必须停止说明。
6. 建立短口令规则：继续当前任务、按当前计划开发、进入下一阶段、按这个开发计划执行、小改、只做小改、按小改流程执行。
7. 建立每次继续强制重读规则，不能只依赖聊天上下文，不能只读开发计划。
8. 建立当前任务状态机：ready -> in_progress -> waiting_user_confirm -> ready -> completed。
9. 建立 UI/动画自动识别、后端/数据/权限/部署自动识别、文档同步和验证规则。
10. 建立长期上下文防漂移机制：每次继续任务前必须重读项目目的、业务边界、项目特殊规范、当前任务状态；涉及 UI 时必须重读 UI 基线。
11. 建立精准上下文读取规则：A0/A1 必须原文读，A2 必须看真实代码和配置，A3/A4 只能用于导航或按需读取。
12. 建立证据链开工检查：项目目的、当前阶段、需求、设计、UI、后端/数据/权限/部署、实际代码、验证依据都要可追溯。
13. 建立实时进度标注：聊天中短频更新，AI当前执行任务.md 中记录关键里程碑。
14. 建立读取升级规则：计划、代码、设计、UI、测试或用户新要求不一致时，必须扩大读取范围；仍不确定则停止确认。
15. 建立开发计划执行前完备性门禁：有开发计划不等于可以开工，必须检查必备文档、必备条件和用户确认状态。
16. 缺少必备条件时，AI 必须每轮提出 1-3 个关键问题，更新相关文档后重新检查，直到门禁通过。
17. 建立小改快速通道：用户显式使用“小改”短口令后，AI 必须先判定是否满足低风险小改条件；通过后使用 AI小改任务.md 限定范围、验证和回滚；不通过则升级为标准开发计划流程。
18. 当前任务状态文件和小改任务卡必须包含 YAML frontmatter，便于 AI 和脚本稳定读取。
19. 初始化完成后运行文档级校验，说明创建/更新文件、未覆盖项、未确认项和后续使用方法。

只初始化规范和文档，不改业务代码。
所有生成或维护的 Markdown 文档统一使用 UTF-8 编码。
```
